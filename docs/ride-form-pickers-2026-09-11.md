# 路线发布日期、时间与地点选择

## 使用方式

- 司机发车、乘客求车与路线列表共用中文月历模板和控制逻辑；发布页确认按钮为“确定日期”。日期下显示青绿“X发”、橙色“X求”，统计当前城市和已选出发地、目的地的两种路线，保留原有提交时的时间校验。
- 时间改为同样式的底部弹窗，24 小时制，可选 00–23 小时、00–59 分钟，保留任意分钟和模板时间。修改草稿后确认才写入表单，取消保留原值。
- 发布表单的出发地、目的地改为可搜索面板。常用地点使用原配置中的 Fort Lee、哥大，保留核心区/全区域等价格键；“其他”入口改为“自选”，直接在同一面板填写。
- 推荐区只显示当前有效路线中其他用户填过的自选地点。排除本人发布、已结束/取消、超过原列表过期宽限、被双向拉黑的路线，以及固定地点别名；精确匹配固定名称，不排除“Fort Lee 某公寓”之类的自选地址。
- 首页社区数据旁的秒数改为小字“我要求车”，直接进入 `newTrip?mode=passenger`。登录/完善资料后保留乘客模式；社区数据原 24 小时缓存保留，移除秒数计时器。

## 实现与调用

- `utils/rideCalendarPicker.js`、`templates/ride-calendar.wxml`、`styles/ride-calendar.wxss` 供列表和发布页共用；月历成功结果缓存 5 分钟，同一次小程序运行中，路线列表、司机发车、乘客求车按相同身份、月份、城市、类型和地点筛选跨页面复用，并合并在途请求。打开或翻月命中缓存时不新增请求；缓存从成功返回时计时，重复使用不续期。
- 路线变更版本、身份、筛选或当天日期改变时使用新的缓存键；失败可重试。关闭或卸载页面不会取消供其他页面复用的读取，迟回包不更新已离开的页面。仅统计数据共享，选中日期与表单草稿各页独立；加入、发布等操作仍由后端实时校验。
- 时间、地点分别为 `ride-time-picker`、`ride-place-picker` 组件。自选输入最多 200 字，地点确认后仍经过原来的价格计算流程。
- 复用 `getTripList` 的 `action: places`，只返回 `{fromPlaces, toPlaces}` 两组地址。一次获取两个方向，按城市、身份和路线变更版本缓存 5 分钟并合并在途请求；失败允许重试，迟回包不能覆盖新页面或身份。
- 后端以 100 条为批次读取可见路线，只取汇总和拉黑检查所需字段，不返回路线、成员、联系方式等资料。每侧推荐最多 100 个，按出现频率及名称排序。

## 验证

- `node --test tests/*.test.cjs`：340 项全部通过。覆盖日期/时间草稿确认与取消、任意分钟、乘客入口与登录回跳、地点输入及推荐缓存、请求乱序和双向拉黑；新增 7 项共享日历缓存测试，验证三页复用、5 分钟过期边界、并发合并、身份/筛选隔离、卸载与失败重试，以及缓存容量限制。
- 微信开发者工具发布页 WXML/WXSS 编译通过。实际验证首页“我要求车”进入乘客模式；司机、乘客月历均读取云端每日统计，确认日期正确写回；时间组件确认 `16:07` 后保持分钟精度。
- 地点推荐实际显示法拉盛、演唱会、EWR 机场等当前路线中的自选地点，常用选项和“自选”入口保留在上方。验证未发布测试路线或写入业务数据。
- 2026-09-11 增量部署现有 `getTripList/index.js` 成功（`execution_success`，任务 `confirmation_cloud_fn_inc_deploy_a16b4716-f90b-4aeb-b8c8-7d71b52856a9`）。此前一次部署因开发者工具登录过期而失败，重新登录后完成。客户端改动仍需随版本上传发布。
